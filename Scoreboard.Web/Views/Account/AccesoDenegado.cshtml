@{
    ViewData["Title"] = "Acceso denegado";
}
<div class="container" style="min-height:60vh; display:flex; align-items:center; justify-content:center;">
  <div class="card p-4 shadow-sm text-center" style="max-width:700px;">
    <h3 class="mb-3">Acceso denegado</h3>
    <p>No tienes permisos para ver este recurso.</p>
    <div class="mt-3">
      <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Volver al inicio</a>
    </div>
  </div>
</div>